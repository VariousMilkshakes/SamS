var ls = require('../../lib-cov/commands/ls');
var assert = require('assert');

describe('ls', function () {
  it('should get the current config', function (done) {
    ls()
    .then(function (config) {
      assert(config);
      done();
    })
    .fail(done);
  });
});
