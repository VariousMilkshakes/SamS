#!/usr/bin/env node

var OrchestrateMongo = require('./lib');

var watcher = new OrchestrateMongo();

watcher.on('connect', console.log);
watcher.on('change', console.log);
watcher.on('error', console.log);
watcher.on('drain', console.log);
